import json
import random

# 加载 JSON 文件


# 示例使用
if __name__ == "__main__":
    file_path = "cookies.json"  # 指定 JSON 文件路径
    random_cookie = get_random_cookie(file_path)
    print(f"随机选择的 Cookie: {random_cookie}")
